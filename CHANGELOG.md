# Changelog

版本遵循 Semantic Versioning。候选版本使用 -rc.N 后缀；正式版本只包含已经通过完整 Windows 回归矩阵的候选版本。

## Unreleased

### Added

- Windows OCR 回退改为运行在可终止的独立 worker 进程中，超时后不会遗留主进程线程或 WinRT 资源。
- 截图编辑器新增标注选择工具：可移动画笔、直线、箭头、矩形、马赛克和文字，调整直线/箭头端点与矩形/马赛克四角，修改选中对象颜色、删除对象，并双击重新编辑文字。
- 标注历史新增最多 100 步 Redo，支持 `Ctrl+Y` 和 `Ctrl+Shift+Z`；新编辑会清空重做分支。

## 0.3.0-rc.4 - 2026-08-24

### Added

- 截图编辑器新增 `ARROW` 工具和 `A` 快捷键；箭头使用当前颜色、3 px 线宽以及固定 12 px、30° 的箭头翼，预览、复制和置顶贴图共用相同输出。

### Changed

- 截图编辑工具栏改用紧凑图标按钮，并为马赛克和 OCR 使用更易辨识的专用图标。

### Fixed

- 截图选区和编辑阶段的右键操作统一为取消当前截图，不再意外复制输出；置顶贴图仍可用右键关闭。

## 0.3.0-rc.3 - 2026-08-24

### Added

- 截图编辑器新增 `MOSAIC` 工具和 `M` 快捷键，可拖动框选多个局部区域并以固定 12×12 物理像素方块遮挡；预览、复制和置顶贴图共用相同结果。

### Fixed

- 文字标注支持在中英文混排文本中用左右方向键移动插入光标，并在光标处输入或退格；光标测量不再把 UTF-16 终止符计入前缀宽度。
- 多个置顶贴图收到同一次 Escape 按键时，只关闭鼠标下方的目标贴图。
- 置顶贴图窗口在隐藏租约之外尽力启用 Windows 捕获排除，降低桌面合成时序导致贴图进入截图的风险。
- 置顶贴图从截图开始到截图会话结束始终隐藏，不再浮在选择、编辑或 OCR 界面上方。

## 0.3.0-rc.2 - 2026-08-19

### Changed

- OCR 改为带会话 ID 的异步操作，识别期间事件循环和置顶贴图保持响应。
- Windows OCR 回退增加独立等待上限，整个 OCR 操作增加总截止时间。
- OCR 完成、失败、超时和取消使用明确协议；取消或过期会话的结果不会影响当前截图。
- 设计文档按问题边界、数据状态、操作契约、模块接口、质量恢复、方案决策和 C4 视图重写。

### Known limitations

- Windows OCR 超时后，隔离线程可能继续执行一段时间，但结果会被丢弃且不会改变当前会话。
- 多显示器、混合 DPI、IME 和外部应用剪贴板消费仍需真实交互式 Windows 回归；本版本保持候选版本状态。

## 0.3.0-rc.1 - 2026-08-18

### Changed

- 深化截图操作、交互、输出、贴图集合、捕获尝试、OCR、剪贴板发布和临时产物生命周期边界。
- 贴图在截图像素捕获完成后立即恢复，不再覆盖后续同步 OCR 时间。
- 贴图改为双击或右键关闭，不显示窗口关闭按钮。
- 剪贴板发布增加双格式部分成功、有限重试和事务确定性。

### Added

- GitHub Actions Windows 强制质量门禁。
- 连续捕获、OCR 制品和剪贴板消费 Release 烟测。
- 统一稳定错误码和隐私安全诊断导出。
- 版本化 Windows 回归矩阵及 Release 结果记录。

### Known limitations

- 多显示器、混合 DPI、IME 和贴图交互仍需在真实交互式 Windows 会话执行版本化回归矩阵。
- 当前候选版本没有自动更新器和代码签名。
